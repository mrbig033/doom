# -*- mode: snippet -*-
# name: general use-package
# uuid:
# key: gup
# condition: t
# --
:general
(:keymaps '(${1:keymap}-mode-map$2)
 :states  '(${3:$$(yas-choose-value '("normal" "visual" "insert"))})
 "$4"     '$0)
